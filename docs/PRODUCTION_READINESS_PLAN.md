# Авро — План готовности к продакшену и чек-лист проверки

> Мастер-план: что осталось сделать до запуска + профессиональный чек-лист
> проверки приложения (всё подтверждается ПО КОДУ, не на словах).
> Роль техлида: каждый пункт закрывается только после фактической проверки в
> коде/тестах, а не по отчёту «готово».
> Дата: август 2026.

---

## Легенда статусов
- ✅ DONE — сделано и проверено в коде/на живой БД
- 🔧 IN PROGRESS — в работе у агента
- ⬜ TODO — не начато
- 🔒 BLOCKED — заблокировано организационным/интеграционным условием

---

## ✅ Уже закрыто (проверено в этой сессии, построчно в коде)
- final_price — сервер источник итоговой цены, число водителя только валидируется (допуск 1₽), 6 тестов зелёные.
- Дыра №1 Выплаты — double-spend/минус-баланс закрыты (резерв outstanding, проверка в ApprovePayout, failed не одобряем, CHECK≥0), integration-тесты зелёные на живой БД.
- Баг orders.notes — миграция добавлена (idempotent, IF NOT EXISTS).
- Дыра №2 Возвраты — мёртвый write-путь refund удалён (в продукте возвратов нет, только отмена до завершения), read-часть и таблица целы.
- git-уборка + fimport — накопленное разложено на коммиты, запушено.
- Главный экран — авто-адрес через backend reverse-geocode (новый эндпоинт), карта на весь экран (extendBody), атрибуция OSM.

---

## ФАЗА 0 — Дочинить то, что в работе
- 🔧 **SOS-кнопка** — удержание 3с не активирует (tap-распознаватель вместо hold). Промпт агенту выдан. Проверить: удержание открывает OfflineSosScreen, раннее отпускание отменяет, кольцо видно, тост убран.
- ⬜ **Остальные экраны с прямого Nominatim/OSRM → на backend** (osm_location_picker, service_detail_screen, map_provider/tracking). То же архитектурное нарушение, что чинили на главном экране. Backend reverse-эндпоинт уже есть.

---

## ФАЗА 1 — Оставшиеся баги из deep-аудита (по критичности)

### HIGH
- ⬜ **Блокировка водителя не снимает с линии.** Reject/block пишет только driver_verifications.status: заблокированный сохраняет активный заказ и продолжает получать офферы. НУЖНО РЕШЕНИЕ владельца: активный заказ переназначить или отменить. Критично для запуска.
- ⬜ **Валидация PUT /admin/settings.** Цену подписки можно поставить 0/отрицательную (зажата только комиссия). Задать границы min/max для day/week/month.
- ⬜ **Batch approve пропускает валидацию ТС**, которую делает single approve.
- ⬜ **Удаление зоны обслуживания → 500 через FK** вместо чистого 409.

### MEDIUM/LOW
- ⬜ Повторный approve уже отклонённого водителя (нет «must be pending»).
- ⬜ Card charge до локального insert → orphan-запись.
- ⬜ Коллизия write-path верификации (админ Upsert по id vs апп Create по user_id → 500).
- ⬜ Утечки сырых ошибок наружу; review 500 вместо 404; отзывы читаемы любым авторизованным.
- ⬜ minimumWithdrawal не enforced на выплатах.
- ⬜ MarkPayoutPaid — добавить явную проверку баланса (сейчас подстраховано только DB CHECK).

---

## ФАЗА 2 — Полный аудит проекта (verification, всё по коду)

### 2A. Graphify — карта архитектуры
- ⬜ `graphify update .` — освежить граф.
- ⬜ `graphify query` по ключевым доменам (order, payment, driver, matching) — связи, god-nodes.
- ⬜ Найти мёртвый код / неиспользуемые эндпоинты / орфанные функции (как refund write-path, как Preview-роут).
- ⬜ Проверить связность слоёв Clean Architecture (domain не зависит от infra и т.д.).

### 2B. Backend — корректность
- ⬜ go build ./..., go vet ./..., go test ./... — всё зелёное.
- ⬜ Integration-тесты (postgres, testcontainers) — все PASS.
- ⬜ **Денежные инварианты**: settlement, комиссия 15%, split (price = driver_amount + commission_amount), available/pending/debt разделены и не смешиваются, идемпотентность денежных операций.
- ⬜ **Стейт-машина заказа**: все переходы; отмена на КАЖДОМ этапе освобождает водителя и возвращает в пул; completed терминален.
- ⬜ **Гонки**: accept_order (один победитель), payout (уже покрыт), wallet parallel credit, webhook dedup.
- ⬜ **Migration drift — полный проход**: сверить схему всех таблиц с кодом репозиториев (не только orders.notes — проверить все INSERT/SELECT колонки против миграций).

### 2C. Безопасность
- ⬜ JWT-auth на всех защищённых роутах; admin-RBAC без эскалации до admin.
- ⬜ Нет SQL-инъекций (всё параметризовано) — подтвердить grep/аудитом.
- ⬜ Секреты НЕ в репозитории/клиенте: JWT_SECRET, ADMIN_PASSWORD только в env; данные карт НЕ хранятся ни во Flutter, ни в backend.
- ⬜ **Rate limiting на OTP-эндпоинтах** — СЕЙЧАС ОТСУТСТВУЕТ, риск SMS-спама (реальные деньги). Критично до публичного запуска.
- ⬜ Валидация входных данных на всех публичных эндпоинтах (границы, типы).
- ⬜ Верификация подписи вебхуков провайдера (YooKassa forged-IP тест уже есть — расширить).
- ⬜ CORS/заголовки безопасности на admin-web.
- ⬜ Проверить, что stub-режимы (S3_STUB_MODE, YOOKASSA_STUB_MODE) не включаются случайно в проде.

### 2D. Устойчивость сервера / нагрузка
- ⬜ Нагрузочный тест ключевых эндпоинтов (создание заказа, pricing/calculate, диспетчеризация) — латентность, деградация.
- ⬜ Таймауты и ретраи HTTP-клиента (30с) — поведение при недоступности провайдера/БД.
- ⬜ WebSocket: переподключение при разрывах, heartbeat, поведение при потере соединения.
- ⬜ Пул соединений БД, поведение Redis при недоступности (graceful degradation).
- ⬜ Проверить, что фоновые задачи (release pending balances, reaper'ы) идемпотентны и не падают на пустых данных.

### 2E. Frontend — UI/UX аудит
- ⬜ flutter analyze --fatal-infos — чисто.
- ⬜ flutter test — все виджет-тесты зелёные.
- ⬜ **UI-аудит всех экранов** (клиент + водитель): консистентность дизайн-системы (Inter, палитры AvroClientColors/AvroDriverColors, без хардкод-цветов), честные состояния (загрузка/ошибка/пусто), нет мёртвого UI («Скоро» без действия).
- ⬜ RU-локализация везде, без англ. заглушек и «lorem».
- ⬜ **Все пользовательские сценарии** (ручной прогон, задокументировать):
  - Клиент: онбординг → auth/SMS → главная → создание заказа (pickup/dropoff/тип/цена) → трекинг статусов → оплата (наличные/карта) → отзыв.
  - Водитель: онбординг → загрузка документов → модерация (pending→approved/rejected) → онлайн-тумблер → приём заказа → активный заказ → завершение → заработок/баланс.
  - SOS: удержание → экран, координаты, звонок 112.
- ⬜ Проверка на реальном iPhone (основная платформа, ~80% пользователей iOS) + гейт Android позже.

### 2F. End-to-end
- ⬜ Полный цикл заказа клиент+водитель на эмуляторе (создание → оффер → принятие → прибытие → в работе → финализация → оплата → завершён).
- ⬜ Живое тестирование диспетчеризации (matching водителя и заказа).

---

## ФАЗА 3 — Подготовка к Cyclops / Точка Банк (код-подготовка)
> Полное исследование: docs/TOCHKA_CYCLOPS_RESEARCH.md
- 🔒 **ADR: источник истины по балансу** — банк (виртуальные счета Cyclops) vs наш ledger (driver_wallets). Решить до реализации.
- 🔒 **TochkaBankClient** реализовать под существующий PaymentProvider-интерфейс (арх. уже provider-agnostic), stub-режим до live.
- 🔒 **Авто-создание бенефициара + виртуального счёта** при одобрении модерации водителя (идемпотентно, с ретраями, маппинг driver_id → beneficiary_id → virtual_account_id).
- 🔒 **Фоновый идентификатор платежей** (list_payments identify:false → identification_payment) — чтобы не задеть 96ч лимит.
- 🔒 **Сплит сделки** (create_deal + execute_deal): commission 15% на счёт площадки + payment_contract 85% водителю; опц. ndfl_to_virtual_account для налогов самозанятых.
- 🔒 Подписание запросов (RSA/ГОСТ), ключ только на backend.
- **Блокеры**: регистрация ИП, статический IP (VPS), доступ от банка (public-api@tochka.com).

---

## ФАЗА 4 — Продакшен-готовность (интеграционные блокеры)
- 🔒 **SMS-шлюз** — без провайдера OTP в проде недоставляемы (провайдер выбран, аккаунт отложен).
- ⬜ **Rate limiting OTP** (дублируется с 2C — сделать до запуска).
- 🔒 **Эквайринг карта/СБП** — сейчас UI-плейсхолдер (ждёт провайдера).
- 🔒 **Миграция на VPS** — статический IP (требование Cyclops).
- ⬜ **Sentry-мониторинг** — подключить.
- ⬜ **FCM push + RuStore-fallback** — ненадёжно на RU-телефонах без fallback.
- ⬜ **Экран покупки подписки водителя** + тумблер подписка/комиссия.
- ⬜ **Deploy-чеклист**: миграции применяются на свежей БД без ошибок, rollback-триггеры, env-переменные заданы, CI зелёный.
- ⬜ Финальный живой E2E-тест диспетчеризации на проде/стейджинге.

---

## Принцип приёмки (для всех фаз)
Задача НЕ принята, пока не подтверждено фактически:
```
[ ] Требуемая функциональность реализована
[ ] Архитектура Авро не нарушена (backend=источник истины, деньги на сервере, секреты не в клиенте, карты/адреса через backend)
[ ] Бизнес-логика не нарушена (комиссия 15%, разделение балансов)
[ ] Нет очевидной регрессии
[ ] Добавлены необходимые тесты, тесты проходят
[ ] Проверено ПО КОДУ техлидом, не по отчёту агента
[ ] Документация обновлена при необходимости
```
